services:
  gateway:
    build: ./gateway
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - 3000:3000
    networks:
      - ctf

networks:
  ctf:
    name: ctf_network
